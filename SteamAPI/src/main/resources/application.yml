spring:
    application:
        name: steam-api
    config:
        import: "optional:configserver:http://config-server:8088"
        fail-fast: true
        retry:
          max-attempts: 5
          initial-interval: 5000
          max-interval: 5000
          multiplier: 1.0

api:
  key: a735e682f615ce8cca4e057cb8a0b526412bde6b